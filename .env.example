# Copy to .env and paste your key (no quotes). Restart the dev server after editing.
# On Vercel: Project → Settings → Environment Variables → VITE_ANTHROPIC_API_KEY (Production + Preview) → Redeploy.
VITE_ANTHROPIC_API_KEY=
